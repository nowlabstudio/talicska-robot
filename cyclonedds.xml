<?xml version="1.0" encoding="UTF-8" ?>
<!--
  CycloneDDS configuration for Talicska robot (Jetson Orin Nano)

  Auto-detect mode: nincs explicit <Interfaces> lista.
  CycloneDDS csak az UP állapotú interfészeket veszi fel → ha enP8p1s0 DOWN
  (Foxglove laptop kábel kihúzva), a stack enP1p1s0 + WiFi-n folytatja.

  - MaxMessageSize: 65500B — RPLidar A2 scan message elfér egy UDP datagramban.
  - Discovery: ParticipantIndex auto, max 32.

  Network layout:
    Jetson enP1p1s0: 10.0.10.1/24  — robot internal (RP2040s, RoboClaw)
    Jetson enP8p1s0: 192.168.68.x  — lab LAN (dev laptop, Foxglove) — opcionális
    lo:              127.0.0.1      — intra-host DDS (konténerek közt)

  Usage:
    export CYCLONEDDS_URI=file:///path/to/cyclonedds.xml
    (docker-compose .env-ből kerül beállításra)
-->
<CycloneDDS>
  <Domain>
    <General>
      <!-- Nincs explicit Interfaces lista → auto-detect, DOWN interfészek kihagyva.
           Fix: 2026-05-09 — hardcoded enP8p1s0 DOWN esetén minden node crashelt. -->
      <AllowMulticast>true</AllowMulticast>
      <MaxMessageSize>65500B</MaxMessageSize>
    </General>

    <Discovery>
      <!-- Restrict discovery to lab subnet -->
      <ParticipantIndex>auto</ParticipantIndex>
      <MaxAutoParticipantIndex>32</MaxAutoParticipantIndex>
    </Discovery>

    <Tracing>
      <!-- Set to "warning" or "info" for debugging; "none" for production -->
      <Verbosity>warning</Verbosity>
      <OutputFile>/tmp/cyclonedds.log</OutputFile>
    </Tracing>
  </Domain>
</CycloneDDS>
